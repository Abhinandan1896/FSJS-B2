import  React  from "react"
import  ReactDOM  from "react-dom/client"
import App from "./App"

const root=ReactDOM.createRoot(document.getElementById('root'))
root.render(
    <App/>
)
//rendering to webpage i.e on document page of index.html 
//render always take what and where so here what is <App/> and where is on ROOT
