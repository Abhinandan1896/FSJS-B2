function setState(){
    let score =22;
    return score;
}
let myscore=setState();
console.log(myscore);
myscore=12;
console.log(myscore);
  
//Primitive data types eg- numbers string, boolean we actually get copy so whenever we change it, its original value doesn't change because we get its copy
//Non-Primitive data types eg- arrays, objects we call these by referance so whenevr we change value the value actually changes...

